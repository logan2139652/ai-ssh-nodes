# WorkBuddy Skill — 导入根目录 SKILL.md

WorkBuddy 版本的 `ai-ssh-nodes` Skill 定义在项目根目录的 `SKILL.md` 中。

直接导入该文件即可，**不要导入本文件**（本文件仅作索引用途）。

> 根目录 SKILL.md 包含完整的 WorkBuddy frontmatter（name / version / description / keywords / agent_created）和中文操作指南。
